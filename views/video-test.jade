extends layout

block content

  div.container(style="margin-top:120px;margin-bottom:120px;")

    h2(style="color:white;")
      | #{(((3*(parseInt(url_params["video_id"])-1)))+1)} - #{(((3*(parseInt(url_params["video_id"])))))} of #{data['work'].length}
      a(style="margin-left:50px;font-size:80%;",href=(parseInt(url_params["video_id"])-1))= "<< prev"
      | —
      a(style="font-size:80%;",href=(parseInt(url_params["video_id"])+1))= "next >>"



    for v,i in data["work"]
      
      if ( (i > (3*(parseInt(url_params["video_id"])-1))) && (i <= (3*(parseInt(url_params["video_id"])))))
        
        video(style="float:left;",src="http://totalmediagroup.s3.amazonaws.com/video/#{v['id']}/#{v['video'][1][1]}.mp4",width="320",height="180",controls="controls")


    for v,i in data["work"]
      
      if ( (i > (3*(parseInt(url_params["video_id"])-1))) && (i <= (3*(parseInt(url_params["video_id"])))))
        div(style="width:320px;float:left;padding-top:30px;color:white;")= v['id']
              